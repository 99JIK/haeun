---
sidebar_position: 4
title: Database Design
---

# Database Design

대학 도서관 관리 시스템 데이터베이스 설계

## 개요

| 항목 | 내용 |
|------|------|
| **기간** | 2025.03 ~ 2025.06 |
| **과목** | 데이터베이스 설계 |
| **역할** | DB 설계, 쿼리 최적화 |
| **기술 스택** | MySQL, ERD, SQL |
| **상태** | 🟢 완료 |

## 프로젝트 배경

데이터베이스 수업 기말 프로젝트로, 대학 도서관의 도서 대출/반납 시스템을 설계했다. 요구사항 분석부터 ER 다이어그램, 정규화, 물리적 설계, 쿼리 최적화까지 전 과정을 수행했다.

## 요구사항

- 도서 정보 관리 (ISBN, 제목, 저자, 출판사, 카테고리)
- 회원 관리 (학생/교직원 구분, 대출 권한 차등)
- 대출/반납 처리 (대출 기한, 연체 관리)
- 예약 기능 (대출 중인 도서 예약)
- 통계 (인기 도서, 월별 대출 현황)

## 설계 과정

### 1. ER 다이어그램

주요 엔티티:
- **Book**: 도서 정보
- **Member**: 회원 (학생, 교직원)
- **Loan**: 대출 이력
- **Reservation**: 예약 정보
- **Category**: 도서 분류

관계:
- Member (1) --- (N) Loan
- Book (1) --- (N) Loan
- Member (1) --- (N) Reservation
- Book (N) --- (M) Category

### 2. 정규화

| 단계 | 적용 내용 |
|------|-----------|
| 1NF | 반복 그룹 제거 (저자를 별도 테이블로 분리) |
| 2NF | 부분 함수 종속 제거 |
| 3NF | 이행 함수 종속 제거 (출판사 정보 분리) |

### 3. 인덱스 설계

```sql
-- 자주 검색되는 컬럼에 인덱스
CREATE INDEX idx_book_title ON Book(title);
CREATE INDEX idx_book_isbn ON Book(isbn);
CREATE INDEX idx_loan_member ON Loan(member_id, return_date);
CREATE INDEX idx_loan_book ON Loan(book_id, loan_date);
```

### 4. 주요 쿼리

**인기 도서 Top 10:**
```sql
SELECT b.title, COUNT(l.id) AS loan_count
FROM Book b
JOIN Loan l ON b.id = l.book_id
WHERE l.loan_date >= DATE_SUB(NOW(), INTERVAL 1 MONTH)
GROUP BY b.id
ORDER BY loan_count DESC
LIMIT 10;
```

**연체 회원 조회:**
```sql
SELECT m.name, m.email, b.title, l.due_date,
       DATEDIFF(NOW(), l.due_date) AS overdue_days
FROM Loan l
JOIN Member m ON l.member_id = m.id
JOIN Book b ON l.book_id = b.id
WHERE l.return_date IS NULL
  AND l.due_date < NOW()
ORDER BY overdue_days DESC;
```

## 성능 최적화

- 대출 이력 테이블에 복합 인덱스 적용 → 조회 속도 약 4배 향상
- EXPLAIN으로 실행 계획 분석 후 풀 테이블 스캔 제거
- 자주 쓰는 통계 쿼리를 뷰(View)로 생성

## 배운 점

- 정규화의 이론과 실제 적용 사이의 갭 (때로는 역정규화가 합리적)
- 인덱스 설계가 쿼리 성능에 미치는 영향이 생각보다 크다
- EXPLAIN으로 쿼리 실행 계획을 분석하는 습관의 중요성
- 트랜잭션 격리 수준에 따른 동시성 제어 이해
